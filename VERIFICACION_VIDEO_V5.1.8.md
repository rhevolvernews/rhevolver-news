# Rhevolver CMS v5.1.8 — persistencia robusta de video

- Guarda cada URL de video como marcador HTML estable dentro de `news.content`.
- Muestra una vista previa visible en el formulario antes de publicar.
- Recupera videos desde `<video>`, atributos antiguos, marcadores y URLs directas.
- No requiere una columna nueva en Supabase.
- La página pública renderiza el reproductor fuera del HTML del editor.
